# P0-0 输入端交互式向导 — 设计方案 (2026-08-03)

> 目标: `python run.py`（终端、无参数）→ 全程中文问答完成建模，用户无需写任何文件、无需接触 Gmsh 语法。
> 本方案只描述设计与接口；实现前先经用户确认。

---

## 1. 现状审计（代码事实）

**已有可复用的零件**：
- `cli.ask()` — EOF 安全输入（`bc_apply.py` 交互、`_resolve_geo_lc` 已用）
- `cli.is_batch_mode()` — 批处理判定（stdin 非 tty 或已提供 BC/载荷 → 不交互）
- `bc_apply._interactive_edge_index` / `_print_segment_menu` — 交互选边

**问题**（本次审计结论）：
1. 提问分散在 3 个模块（input_source 问 lc / bc_apply 问 BC / runner 问文件），无统一流程
2. 几何必须先有文件 — 无纯交互建几何入口；裸 `run.py` 只问一句"输入文件:"，不知道格式的用户直接卡死
3. BC 交互发生在网格生成**之后**、靠边编号菜单 — 用户必须先跑一次网格才知道边号
4. 无最终总览/确认环节 — 答错要到 FATAL 才发现（违背"静默错误优先变响亮"）
5. 边名合法性不预防 — `圆板 左 固定` 要到生成期才被拒（round-3 已修成报错，向导应直接从源头消灭）

## 2. 交互流程（问答顺序）

```
S1 几何: 类型 (矩形板/圆板/圆环/带孔矩形板) → 尺寸 → 内孔 (可多条, 回车结束)
S2 网格: 密度 lc [默认 0.1]
S3 材料: E [2.1e11] / ν [0.3] / 厚度 [0.01]   (回车=默认)
S4 边界: 循环 — 打印可用边名列表 → 类型 (固定/压力/拉力) → 数值; 回车结束
S5 体力: bx,by [回车跳过]
S6 总览表 + y/n 确认 (n → 回到 S1)
S7 保存模型为 .txt? [y] → 写回用户资产格式
→ 生成临时 .geo → 走现有 resolve_geo 管线求解
```

**关键设计: 边名白名单按形状**（与 geo_spec 的 Physical Curve 命名一致，向导只问合法边 → `圆板 左` 类错误从源头消失）：

| 形状 | 可用边 |
|------|--------|
| 矩形板 | 左 / 右 / 上 / 下 |
| 圆板 / 圆环 | 外 / 内 |
| 带孔矩形板 | 左 / 右 / 上 / 下 / 内孔1..N |

**BC 类型与 .txt 同集**：`固定`（无数值）、`压力 p`（法向）、`拉力 t`（t,0）。高级载荷（tx,ty / 剖面 / 集中力）保留 CLI/.spec，向导不扩展新语法。

## 3. 技术方案

- **新模块 `fem2d/wizard.py`**（~300 行）：`run_wizard(config) -> geo_path` — 问答结果构建 spec dict → 复用 `scripts/geo_spec.generate_geo` → 临时 .geo → 返回路径
- **BC 校验零新增**：生成的 .geo 含 `@FEM:` 注解 → 走现有 `merge_geo_fem_config`（round-3 的 22 处校验自动生效）；数值校验复用 `require_positive` 语义，非法立即重问、不静默
- **触发**：`--wizard` 显式；或 无 mesh 参数 + stdin 是 tty → 自动进向导（**替代**现有裸"输入文件:"提问 — 用户钦定方向）
- **批处理安全**：已有任何 CLI BC/载荷参数 → 向导跳过对应阶段（显示"使用 CLI 参数"），几何问题仍问
- **临时文件**：`NamedTemporaryFile` + atexit 清理（与 `_resolve_geo_lc` 同模式）
- 所有回答 NFKC 归一化；EOF → 按回车/默认处理，顶层 EOF 干净退出（现有 ask 语义）

## 4. 兼容策略

- `.txt/.spec/.geo/.msh` 四条输入路径**全部不动**；向导只是"入口收敛"的第 5 条（与 .txt 同内核：spec dict → generate_geo）
- `models/*.txt` 资产不变；S7 保存的 .txt 可被 `run.py models/xxx.txt` 直接复跑
- `run_demo.py` 及测试不受影响（显式传文件路径）
- 入口收敛结果: **交互向导 + 描述文件 两条主路径**，CLI 参数作覆盖层（用户目标达成）

## 5. 测试（tests/test_wizard.py）

- monkeypatch `cli.ask` 用脚本化回答队列：完整 happy path（矩形板 3×2 + 左固定 + 右拉力 1e6 + 体力）→ 生成的 .geo 含正确 @FEM 注解、config 字段匹配
- 非法输入重问（宽=-1 → 重问）；EOF 各阶段 → 默认值/干净退出
- 边名白名单：圆板向导永不问 左/右；带孔板问 内孔1..N
- S7 保存的 .txt 重新解析 == 原 spec（round-trip）
- 判别性：测试只针对新代码，旧实现无此功能

## 6. 本阶段不做

- 复杂几何（.geo 高级用户保留）；交互式绘图编辑；CLI 参数行为修改
- 集中力/剖面面力的交互输入（保持 CLI/.spec 能力边界，后续可加）

## 7. 风险

- 流程较长 → 每步有默认值、回车一路到底
- 向导与 .txt 生成几何必须严格同内核（spec dict 共用）→ 行为不会分叉

---

## ✅ 实现状态 (2026-08-03 已实现)

- **fem2d/wizard.py** (~380 行): 完整交互向导 — 几何(4 形状+孔) → 网格密度 → 材料(E/nu/t 默认值) → 边界(白名单+固定/压力/拉力双分量) → 体力 → 总览确认(可重来) → 可选保存 .txt
- **接线**: `--wizard` CLI 参数; 无 mesh + 终端 → 自动进入 (替代裸"输入文件:"提问); 向导入口含"使用已有文件"选项
- **产物**: spec dict → generate_geo → 临时 .geo (含 @FEM) → 现有 resolve_geo 管线 (零新解析器)
- **安全**: 每答即时校验重问 (负值/NaN/非法边名/重复边); EOF 默认值或干净退出 (无死循环); 临时文件 atexit 清理
- **测试**: tests/test_wizard.py 15 个判别性测试 (完整流程/非法重问/边名白名单/双分量/EOF/round-trip/入口) + 端到端验证 (管道 locale 编码实测: fixed 22 DOFs, surf 10 segs, eta 11.5%)
- 验收: `python run.py` (终端) 或 `python run.py --wizard`
